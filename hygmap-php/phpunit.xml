<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="tests/bootstrap.php"
         colors="true"
         cacheDirectory=".phpunit.cache"
         executionOrder="depends,defects"
         failOnWarning="true"
         failOnRisky="true"
         beStrictAboutOutputDuringTests="true">
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <!--
            Integration tests run against the live API and database (see the
            test-integration target in the Makefile). Without this entry, running
            phpunit with the Integration suite name matched nothing and exited 0,
            which is how an empty directory passed for a test suite for months.
        -->
        <testsuite name="Integration">
            <directory>tests/Integration</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory suffix=".php">src</directory>
        </include>
        <exclude>
            <file>src/index.php</file>
            <file>src/map.php</file>
            <file>src/search.php</file>
            <file>src/configure.php</file>
            <file>src/bootstrap.php</file>
        </exclude>
    </source>
</phpunit>
